extends layout.pug

block content
  section#content
    h1 "Text Content"
  table
    tr
      th "Title"
      th "Author"
      th "Genre"
      th "Year"
    each book in books
      book
      tr
        td= book.title
        td= book.author
        td= book.genre
        td= book.year